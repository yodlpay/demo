app = "demo"

[build]
dockerfile = "Dockerfile.prod"

[[services]]
internal_port = 5000
protocol = "tcp"

  [services.concurrency]
  hard_limit = 25
  soft_limit = 20

  [[services.ports]]
  handlers = ["http"]
  port = 80

  [[services.tcp_checks]]
  interval = 10000
  timeout = 2000
